<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/base.xml"/>
    <property resource="application.yml"/>

    <appender name="ROLLING_SAP" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/bayern_sap.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/bayern_sap-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
           <!--  保留30天 -->
            <maxHistory>30</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <!-- 在SizeAndTimeBasedRollingPolicy中已经实现，此配置可以省略 -->
        <!-- <triggerringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
        	<maxFileSize>50MB</maxFileSize>
        </triggerringPolicy> -->
        <!-- <encoder>
        	<pattern>%msg%n</pattern>
        </encoder> -->
        <layout class="ch.qos.logback.classic.PatternLayout">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{35} - %msg%n</Pattern>
        </layout>
    </appender>

    <logger name="com.qhc.bayern.sap" level="debug"/>
    <root level="INFO">
        <appender-ref ref="ROLLING_SAP"/>
    </root>
</configuration>